import styled from "styled-components";

const StructPage = styled.div`
  width: 100%;
  height: 100%;
`;

const Container = styled.div`
  flex-direction: row;
  height: 50px;
  position: relative;
`;

const LeftWrapperElement = styled.div`
  float: left;
`;

const RightWrapperElement = styled.div`
  float: right;
`;

export { StructPage, Container, LeftWrapperElement, RightWrapperElement };
